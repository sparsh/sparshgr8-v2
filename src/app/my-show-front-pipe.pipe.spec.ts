import { MyShowFrontPipePipe } from './my-show-front-pipe.pipe';

describe('MyShowFrontPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MyShowFrontPipePipe();
    expect(pipe).toBeTruthy();
  });
});
